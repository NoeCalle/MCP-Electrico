name: p13e-motor-starting-dossier

on:
  pull_request:
  workflow_dispatch:

jobs:
  motor-starting-dossier:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            python: "3.11"
          - os: windows-latest
            python: "3.12"
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: Install validated dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt
          pip check

      - name: Validate P13E motor Workspace and dossier
        run: python -m pytest -q tests/test_p13e_motor_starting_dossier.py
