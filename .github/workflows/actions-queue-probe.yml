name: actions-queue-probe
on:
  push:
    branches: [feat/p3c11e-table5e-primary]
    paths: [.github/workflows/actions-queue-probe.yml]
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "actions scheduler probe"
